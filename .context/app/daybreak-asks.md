# Daybreak asks — Reclaim Your Week → Daybreak

The leaf-tier sibling of Daybreak's own
[`upstream-asks.md`](../framework/upstream-asks.md), one tier down. Two kinds of
row live here:

- **Fork-first seams** — framework-tier code this app carried because the seam it
  needs does not exist in Daybreak yet. `upstream`'s push URL is disabled, so the
  change lives here until Daybreak adopts it. On every Daybreak pull
  (`git fetch upstream && git merge upstream/main`), check each **open** row: if
  Daybreak has landed it, **delete our copy and delegate**, then close the row.
- **Defects** — framework bugs this app found by being Daybreak's first real
  consumer. That is a stated goal of this build, not a side effect: F3
  (`ryw-firstlight`) budgets two to three, and anything the build trips over
  afterwards belongs here too.

Without this ledger, a framework-tier change we carry either conflicts on the
next sync or quietly becomes permanent local divergence. That is the whole reason
the tier below keeps the same file.

**This is not a boundary-breach log.** A breach is editing a Daybreak-owned file
because no seam exists — those get a minimal `keep-mine` edit and a follow-up, per
the [`CLAUDE.md`](../../CLAUDE.md) banner. This ledger is the sanctioned case: the
code is clean framework-tier work whose final home is upstream.

## The ledger

| Our change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Feature                                                                                                                                                     | Delegate-when-it-lands action                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`lib/framework/data-slots/values.ts`](../../lib/framework/data-slots/values.ts) — optional `runId?: string` on `SlotValueProvenance`, plus `getSlotHistory(userId, slotSlug)` returning superseded versions alongside the heads `getSlotHeads()` already returns                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [F1 `ryw-provenance`](./planning/plan.md)                                                                                                                   | Drop our copy in favour of Daybreak's (reconcile the signature if it differs); keep the leaf callers in `saveAnswer()` (I3) and the F9 trend-line reads.                                                                                                                                                                                                                                                                                                                 | **open — filed [daybreak#156](https://github.com/human-centric-engineering/daybreak/issues/156)** (2026-07-24). Additive and back-compatible, so nothing breaks meanwhile: per-run provenance and value history are generic facilitation needs, not RYW-specific, and any repeat-audit-shaped app hits the same wall. Close this row on the sync that lands it, once our copy is deleted and callers delegate.                                                                                                                                                                                                                                                                                                                                                                   |
| [`lib/app/eslint.config.mjs`](../../lib/app/eslint.config.mjs) — a leaf block re-permitting `@/lib/framework` imports under the leaf-owned paths — `prisma/seeds/app-reclaim/**` (F3) plus the app surfaces `app/api/v1/app/**`, `app/(protected)/programme/**`, `app/admin/programme/**` (F4). Daybreak's core→framework ban (`lib/framework/eslint.config.mjs`) exempts `scripts/smoke/**` and the framework-tier app routes but not the leaf's OWN paths, which legitimately consume the framework (the leaf is built ON it and always has `lib/framework`; the ban protects Sunrise core, not the leaf tier).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [F3 `ryw-firstlight`](./planning/ryw-firstlight.md) · [F4 `ryw-shell`](./planning/ryw-shell.md)                                                             | When Daybreak folds the leaf-owned paths (`prisma/seeds/app-*/**` + `app/api/v1/app/**`, `app/(protected)/programme/**`, `app/admin/programme/**`) into the ignore list of its core→framework ban, delete our leaf block and the paired assertion in `tests/unit/lib/app/defaults.test.ts`.                                                                                                                                                                              | **open — filed [daybreak#157](https://github.com/human-centric-engineering/daybreak/issues/157)** (2026-07-24). Leaf-side workaround only; no framework code carried. Not urgent — the ban's build-time rationale genuinely does not apply to seeds, so the exemption is correct to hold locally until upstream generalises it.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `prisma/seeds/app-reclaim/002-reclaim-surface.ts` and `scripts/smoke/reclaim.ts` call `initFramework() → initLeafApp() → syncFramework()` directly to materialise the `Module` / slot / capability rows. A standalone `db:seed` (what `db:reset` and CI run) and the smoke never boot the app, so nothing else creates the rows the leaf then activates, links, and binds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [F3 `ryw-firstlight`](./planning/ryw-firstlight.md)                                                                                                         | If Daybreak adds a leaf-seeding seam — a documented `syncFrameworkForSeed()` entry point, or the seed runner booting the framework once before app-tier seeds — replace the inline boot-chain calls with it.                                                                                                                                                                                                                                                             | **open — filed [daybreak#158](https://github.com/human-centric-engineering/daybreak/issues/158)** (2026-07-24). Not a defect: the boot sequence is public and idempotent, and calling it works. It is an ergonomics gap — the first leaf to seed framework _config_ (maps, module activation, bindings) has to reach into boot internals because no seeding seam exists.                                                                                                                                                                                                                                                                                                                                                                                                         |
| **No platform seam to close public self-signup.** Sunrise wires better-auth with `emailAndPassword: { enabled: true }` (`lib/auth/config.ts`) and ships a live `/signup` page, with **no config, env var, or seam** to run the app invite-only — checked: no `disableSignUp` / `ALLOW_PUBLIC_SIGNUP` anywhere in `lib/`, and better-auth 1.6.23's own core types carry `disableSignUp` only on the OAuth link-account options, not on `emailAndPassword`. Sunrise ships a full invitation system (`lib/utils/invitation-token.ts`, `/api/auth/accept-invite`, the admin invite page) whose whole premise is that access is granted rather than taken — but the front door stays open beside it. **The leaf carries no core code:** F8 refuses at its own run-creation gate (I14), which is the whole of the leaf's answer. **The cosmetic nav tidy F8 planned was never built, and would not have worked** — `DEFAULT_PUBLIC_NAV` (`lib/public-nav/types.ts`) is Home / About / Contact and carries no signup link, so `lib/app/public-nav.ts` had nothing to override. The real routes to `/signup` are the "Get Started" CTAs on the landing and about pages (Sunrise starter copy, replaced by post-v1 [[planning/post-v1 | P4]]) and core's signed-out user button. Corrected 2026-07-26 after the close-out audit found this row describing a change that does not exist in the tree. | [F8 `ryw-access`](./planning/ryw-access.md)                                                                                                                                                                                                                                                                                                                                                                                                                              | When Sunrise exposes a signup-mode config (a `SIGNUP_MODE=open \| invite_only` env consumed in `lib/auth/config.ts`, gating both the better-auth sign-up route — a `hooks.before` on `/sign-up/email` — **and** the `/signup` page), set it to `invite_only`. There is no leaf tidy to delete — `lib/app/public-nav.ts` is untouched (all three exports `null`). Keep the run-creation gate regardless: entitlement is ours (I14), and the platform toggle only stops accounts being created, not audits being started.                                                                                                                                                                                                                                                          | **open — filed [sunrise#463](https://github.com/human-centric-engineering/sunrise/issues/463)** (2026-07-25, `upstream-gap` + `enhancement`). Not a defect: an open signup page is the right default for a starter template. It is a **seam gap** for any fork whose product is invite-gated — and the shape that makes it worth filing is that Sunrise already has the invitation half. Found while planning F8, not while building it: every document said the app was invite-gated and the running app was not (lesson in [`planning/planning-retro.md`](./planning/planning-retro.md) §B). |
| **No fork-owned seam at user creation.** `databaseHooks.user.create.{before,after}` (`lib/auth/config.ts`) is wired directly to two core functions, and `/api/auth/accept-invite` creates the user inline — so a fork that must react to a new account (mint an entitlement grant for the accepted invite's tier, record consent, provision a workspace, notify a CRM) has to edit a core auth file. Sunrise reserves a `lib/app/*` scaffold for nearly every other extension point (contributors, capabilities, emails, protected routes, public nav, rate limits, agent fields, env, surface) — **none for the auth lifecycle**. **The leaf carries no core code:** F8 resolves the invite **lazily at its own run-creation gate** (I14), idempotently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [F8 `ryw-access`](./planning/ryw-access.md)                                                                                                                 | When Sunrise adds an auth-lifecycle scaffold (e.g. `lib/app/auth-hooks.ts` exporting `onAppUserCreated`, composed into the existing `userCreateAfterHook`), move invite redemption there so the grant exists **before** the user's first action, and simplify `assertEntitled` back to a pure entitlement check. Keep the gate itself — entitlement is ours (I14).                                                                                                       | **open — filed [sunrise#464](https://github.com/human-centric-engineering/sunrise/issues/464)** (2026-07-25, `upstream-gap` + `enhancement`). Not a defect — the lazy resolution is correct and tested. It reconciles state later rather than establishing it when the account exists, which is the weaker model for anything that must be true before the first action. `UserCreateData` / `DatabaseHookContext` are already exported, so the seam needs no new types.                                                                                                                                                                                                                                                                                                          |
| **`HOOK_EVENT_TYPES` is a closed enum, so no fork or framework tier can emit or subscribe to its own events.** The machinery is string-keyed throughout — `Hook.eventType` is `VarChar(100)`, the dispatcher a `Map<string, …>` — but three `z.enum` / TS sites shut the door: `emitHookEvent`'s signature, `HookEventPayloadSchema` (so retries fail to re-parse), and `createHookSchema` in the admin route (so a fork **cannot even persist** a subscription). **The leaf carries no core code:** F8 t-4 adds a local emitter (`lib/app/programme/access/events.ts`) that logs the two events and is the single wiring point for an ESP later.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [F8 `ryw-access`](./planning/ryw-access.md)                                                                                                                 | When Sunrise opens the type (namespaced custom events, or a `registerHookEventTypes()` seam), delete the local emitter and emit `reclaim.signup` / `reclaim.audit_completed` through `emitHookEvent`, so they reach the webhook delivery, retry, filter and audit machinery that already exists.                                                                                                                                                                         | **open — filed [sunrise#465](https://github.com/human-centric-engineering/sunrise/issues/465)** (2026-07-25, `upstream-gap` + `enhancement`). **The strongest evidence is Daybreak's own:** `lib/framework/facilitation/map/publish-hooks.ts` records that a core publish-event bus "was rejected as a core edit for one internal consumer (decision C)", so the framework tier already runs a **second, parallel** event mechanism for the same reason — and this leaf was about to add a third. The cost of the closed enum is duplicated event plumbing at every tier, none of it reaching the shared delivery infrastructure.                                                                                                                                                |
| `app/api/v1/app/reclaim/runs/service.ts` (`createRun`) creates the `UserJourney` row directly with `prisma.userJourney.create`. `applyEvent`/`applyJourneyTransition` is the sole writer of journey _state_ but takes an existing `journeyId`; nothing in the framework creates a `UserJourney` (F3 needed none). So a leaf that starts a run must write the row itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [F4 `ryw-shell`](./planning/ryw-shell.md)                                                                                                                   | When Daybreak exposes a journey-creation seam (e.g. `createJourney(userId, graphSlug, contextKey)`), replace the direct `prisma.userJourney.create` in `createRun` with it.                                                                                                                                                                                                                                                                                              | **open — filed [daybreak#159](https://github.com/human-centric-engineering/daybreak/issues/159)** (2026-07-24). Not a defect — journey creation is a generic facilitation need (any leaf running a journey hits it), like F1's `runId`. Direct create works meanwhile; delegate on the sync that lands the seam.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `lib/app/programme/runs/answers.ts` queries `prisma.slotValue` **directly** to read one run's values — every version stamped with a `provenance.runId`, superseded or not — because `getSlotHistory(userId, slotSlug)` is **per-slug** and a summary needs dozens of them. There is no batched sibling: `getSlotHeads` takes a `slotSlugs[]` array but is `WHERE supersededAt IS NULL`, and `getSlotHistory` returns every version but only for one slug. So the one read a repeat-audit app needs most — _many slugs, one run, history included_ — is the one combination the module does not offer, and the leaf drops to raw Prisma over a `framework_*` table to get it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [F9 `ryw-repeat`](./planning/ryw-repeat.md) (D1 fix, landed ahead of the feature)                                                                           | When Daybreak adds a batched history read — `getSlotHistory(userId, { slotSlugs?, runId? })`, or a `slotSlugs`/`includeSuperseded` option on the existing pair — replace the raw query in `readRunAnswers` with it and delete the Prisma import. The `provenance.runId` filter belongs in the framework too: F1 t-2 put `runId` on provenance precisely so a run's picture is readable, and every consumer of that field will otherwise write this same query.           | **open — filed [daybreak#162](https://github.com/human-centric-engineering/daybreak/issues/162)** (2026-07-25, `enhancement`). Not a defect, and the missing read is the direct cause of a **real bug this fix closes**: `readRunAnswers` filtered `getSlotHeads` by `runId`, which is correct until a leader has two audits and then silently empties the first one — including the public share link F7 invites them to send to a colleague. `getSlotHistory` was added in F1 t-2 for exactly this and had no consumer until now, so the gap only surfaced when the second run did. Any app whose runs repeat hits it.                                                                                                                                                         |
| No framework code carried — a **documented exclusion**. `fill_slot` (the capability the coach agent uses to capture profile slots mid-conversation) builds provenance from `conversationId` / `moduleSlug` / `nodeKey` with **no `runId`**, and there is no way for a caller to supply one: the journey `contextKey` that identifies a run is the app's, not the capability context's. So in a module whose runs repeat, an agent-captured value structurally cannot belong to a run. Concretely: a leader who fills the Phase 0 form with role "CEO" and later tells the coach they are now COO leaves the correction belonging to no audit, and that audit's summary keeps saying CEO.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [F9 `ryw-repeat`](./planning/ryw-repeat.md) (D1 fix)                                                                                                        | If Daybreak lets a capability write carry the caller's run context (an optional `runId` on the execution context that `fill_slot` copies through, or resolution from the conversation's active journey), decide deliberately whether the coach's profile corrections should join the in-flight run — and delete the paragraph in `answers.ts` explaining why they do not.                                                                                                | **open — filed [daybreak#167](https://github.com/human-centric-engineering/daybreak/issues/167)** (2026-07-25, `enhancement`). Sibling of daybreak#162. Our current behaviour is defensible and is what we want — a run summary is a snapshot of what that run captured — so this is recorded so the next person finds a decision rather than a surprise. Surfaced by `/code-review` on the D1 fix, which made the exclusion visible: before it, a coach correction blanked the field entirely. **Moot for this app from 2026-07-27:** the coach no longer holds `fill_slot` at all, since `record_answers` covers the same group from a server-issued run. Left open rather than closed — the observation is general, and the next leaf to grant `fill_slot` hits it unchanged. |
| **`EmailPropsMap` is a closed interface, so a fork can override an email but never add one.** `lib/email/registry.ts` fixes four auth kinds (`welcome`, `verifyEmail`, `resetPassword`, `invitation`) and derives `EmailKind`/`EmailOverrides` from them — so `lib/app/emails.ts`, the fork-owned scaffold, can replace core's invitation email (F8 t-1 did) and cannot register the quarterly nudge F9 needs. The machinery beneath is generic: `sendEmail` takes any React element. **The leaf carries no core code:** F9 t-3 renders `components/app/emails/quarterly-nudge.tsx` and calls `sendEmail` directly, outside the registry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [F9 `ryw-repeat`](./planning/ryw-repeat.md) t-3                                                                                                             | When Sunrise lets an app contribute kinds (a `registerEmailKind`, or an `AppEmailPropsMap` merged into `EmailKind` by declaration merging), register the nudge through `lib/app/emails.ts` and drop the direct `sendEmail` call, so the email joins whatever preview/suppression tooling the registry grows.                                                                                                                                                             | **open — filed [sunrise#468](https://github.com/human-centric-engineering/sunrise/issues/468)** (2026-07-25, `upstream-gap` + `enhancement`). **The third closed-enum finding in three features** — after `HOOK_EVENT_TYPES` (sunrise#465) and the config descriptor walker (daybreak#161). Same shape each time: a generic mechanism behind a shut type, and every tier ends up with a shadow copy of infrastructure the platform already has.                                                                                                                                                                                                                                                                                                                                  |
| **No seam for an app's own recurring job.** `processDueSchedules()` (`lib/orchestration/scheduling/scheduler.ts`) walks `AiWorkflowSchedule` rows and starts workflow executions; an app job that is not a workflow cannot register. Modelling "email the leaders whose last audit finished ~90 days ago" as a workflow would mean inventing a workflow, a mail-sending capability and a schedule row to wrap three queries. **The leaf carries no core code:** F9 t-3 adds `POST /api/v1/app/reclaim/nudges/tick`, admin-guarded with its own rate-limit sub-cap, driven by external cron — the same pattern core documents for its own tick route.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [F9 `ryw-repeat`](./planning/ryw-repeat.md) t-3                                                                                                             | When Sunrise adds a scheduled-job scaffold (e.g. `lib/app/scheduled-jobs.ts` exporting `{ name, cron, run }` entries that the existing tick executes), register `runNudgeTick` there and delete the leaf route — inheriting the platform's answers on auth, overlap, logging and failure handling instead of maintaining our own.                                                                                                                                        | **open — filed [sunrise#469](https://github.com/human-centric-engineering/sunrise/issues/469)** (2026-07-25, `upstream-gap` + `enhancement`). Not a defect: an app can do this today and ours does. The tell is that the platform's own answer to "how does this run periodically" is already an external cron hitting a route, which is exactly the mechanism an app would want to share.                                                                                                                                                                                                                                                                                                                                                                                       |
| **The module-config descriptor walker renders arrays as a raw JSON textarea, so a module whose content IS an array cannot be edited by the person who owns it.** `lib/framework/modules/config/schema-descriptors.ts` serialises a module's Zod `configSchema` via `z.toJSONSchema()` and walks the result, rendering string / number / boolean / enum and falling back to a raw-`json` descriptor for anything richer — explicitly including arrays and nested objects. The bound is deliberate and the header says so. But for this module the arrays are `buckets` (nine title + description + benchmark-note triples) and `hourBands`, which is to say **the entire body of the coach's content is exactly the part that degrades**. Handing a non-technical owner a JSON blob and calling it content editing is not the capability the generic form appears to offer. **The leaf carries no framework code:** F10 t-4 owns the _form_ and posts to the framework's own `PUT .../config`, so validation, the `ModuleVersion` snapshot, the change summary and the admin audit entry all still happen upstream.                                                                                                           | [F10 `ryw-admin`](./planning/ryw-admin.md) t-4                                                                                                              | When the walker grows array-of-object descriptors (an `items` descriptor plus an index path the client can render rows from), delete `components/app/admin/content/content-editor.tsx` and the leaf `/admin/programme/content` route and point Rashmir at `/admin/framework/modules/reclaim-audit`. Keep the **divergence markers** regardless — those are leaf-specific (they compare against this app's own I11 source chain) and no generic form can know about them. | **open — filed [daybreak#161](https://github.com/human-centric-engineering/daybreak/issues/161)** (2026-07-25, `enhancement`). Not a defect: a bounded walker with a total fallback is the right call for a generic form, and the fallback is documented. It is a **capability gap** that only shows up when a module's config is its content rather than its settings — which is the shape any facilitation module built on Daybreak will tend to have.                                                                                                                                                                                                                                                                                                                         |
| **No data-subject export anywhere in the platform.** Sunrise implements the _erasure_ half of GDPR properly — `eraseUser()`, `DataErasureReceipt`, an erasure-hooks seam, a cascade policy per table — and nothing at all for the _access_ half (Art. 15). The only exports under `app/api/v1` are orchestration admin backups (`agents`, `conversations`, `backup`), none of which is subject-scoped. So every fork holding personal data has to write its own, and each one re-answers the same question about which tables count. **The leaf carries no core code:** F10 t-5 adds `lib/app/programme/admin/export.ts` over its own tables plus `framework_slot_value`, with a test pinning `EXPORTED_SOURCES` to the tables the schema actually declares so a later table cannot be silently omitted.                                                                                                                                                                                                                                                                                                                                                                                                                     | [F10 `ryw-admin`](./planning/ryw-admin.md) t-5                                                                                                              | When Sunrise adds a subject-export service with a registration seam mirroring `getErasureCleanupHooks()` (an app registers what it holds; core assembles and serialises), register the leaf's tables through it and delete the bespoke builder. Keep the completeness test — it is what makes the registration honest.                                                                                                                                                   | **open — filed [sunrise#467](https://github.com/human-centric-engineering/sunrise/issues/467)** (2026-07-25, `upstream-gap` + `enhancement`). Not a defect: erasure is the legally riskier half and shipping it first is the right order. It is a **seam gap** — the erasure side already proves the pattern works, so the access side is a known shape rather than a design question.                                                                                                                                                                                                                                                                                                                                                                                           |

| **No seam for a server-composed opening turn.** `streamChat` takes a required non-empty `message` (`lib/orchestration/chat/types.ts:31`) and persists it as a `role:'user'` `AiMessage` **before** the model is called (`lib/orchestration/chat/streaming-handler.ts:626-630`). Nothing in Sunrise, Daybreak or the `AiAgent` schema has any concept of an agent speaking first — no greeting field, no opener, no first-message. The nearest thing, `widgetConfig.conversationStarters`, is clickable prompts for the _user_ to send. So a facilitation tool whose stated method is "ask before you tell" cannot open its own phase without writing a turn into the leader's transcript that the leader did not write. **The leaf carries no core code:** it sends a self-describing stage direction (`lib/app/programme/coach/opening.ts`) and filters it out of the transcript on rehydrate. | [ryw-conversational](./planning/ryw-conversational.md) stage 4 | When `ChatRequest` grows an opening/seed turn that skips `persistMessage` and injects the text as a developer message, delete `COACH_OPENING_TRIGGER`, `COACH_SYNTHETIC_MESSAGES`, `isCoachSyntheticMessage` and the filter in `coach-chat.tsx#loadTranscript`. | **open — filed [sunrise#474](https://github.com/human-centric-engineering/sunrise/issues/474)** (2026-07-27, `upstream-gap` + `enhancement`). Sibling of sunrise#475 below. Not a defect: an agent-first turn is simply a case the chat handler was never asked to serve. It becomes structural the moment a fork's product is a facilitated conversation rather than a support chat, which is the whole of Daybreak's remit. |
| **`AiMessage.metadata` is `Json?` and only the framework writes it.** `persistMessage` (`streaming-handler.ts:2343-2369`) populates it from a framework-controlled `MessageMetadata`, and `ChatRequest` exposes only `costLogMetadata`, which lands on `AiCostLog` rather than the message row. So a leaf cannot mark a turn as synthetic, system-triggered or hidden anywhere except **inside the message text** — which is the direct cause of the sentinel string in the row above. The alternative is a leaf writing to a core-owned table after the fact. | [ryw-conversational](./planning/ryw-conversational.md) stage 4 | When a leaf can attach its own namespaced metadata to a message it caused, tag the opening turn there and filter on the tag rather than on an exact string match. | **open — filed [sunrise#475](https://github.com/human-centric-engineering/sunrise/issues/475)** (2026-07-27, `upstream-gap` + `enhancement`). Cross-referenced with sunrise#474 above: either one landing removes the workaround, and the pair is stronger evidence than either alone that "an app caused this turn for its own reasons" has nowhere to live. |
| **No leaf-reachable writer for per-phase run progress.** `UserNodeState.progress` is `Json?` and documented as "optional module-defined progress payload… shape owned by the node's module, opaque to the engine" (`prisma/schema/framework-facilitation.prisma:110`) — semantically the exact home for "this leader has now been shown the picture of their week at this node". There is no way to write it: `applyJourneyTransition` (`guidance.ts:118-131`) takes only `{ nodeKey, kind }`, `TransitionRequest` carries no payload, and `JourneyEvent` is inserted inside `applyEvent`'s own transaction. **The leaf carries no framework code:** it added `ReclaimAuditRun.coachOpenings`, a scalar list on its own table. | [ryw-conversational](./planning/ryw-conversational.md) stage 4 | When Daybreak exposes a progress writer (`recordNodeProgress(journeyId, nodeKey, patch)`, or a `progress` field on `TransitionRequest`), move the moment ledger there and drop the column plus its migration. | **open — filed [daybreak#168](https://github.com/human-centric-engineering/daybreak/issues/168)** (2026-07-27, `enhancement`). Not a defect — the field is documented as module-owned and simply has no setter. Worth filing because the shape recurs: any facilitation module with a beat that must happen once per node hits it, and the workaround is a bespoke column per leaf for a concept the framework already models. |
| **`facetAllows` has no slug-level facet, and module capabilities do not inherit exposure enforcement at all.** Two halves of one gap, found together. (1) `facetSchema` (`lib/framework/data-slots/capabilities/exposure.ts`) is `.strict()` on `{ groups, scopes }`, so a group that legitimately mixes computed lanes with leader self-reports — which `reclaim_calendar` does, and which any facilitation module with a computed branch will — cannot express its real permission as data; the slug-level rule has to live in code beside it. (2) `facetAllows` is called by the framework's own `fill_slot` and `get_state` and **by nothing else**, so a leaf that declares a capability on its own `ModuleDefinition` and grants it an `ExposureConfig` gets a config that silently enforces nothing. This app believed for a year that its write allowlist was enforced twice; it was enforced once, in code, and the invariant test asserted against a hand-written mirror of `facetAllows` rather than the function. **The leaf carries no framework code:** `record-answers.ts` now reads `context.customConfig` and calls `facetAllows` itself, and `agent-caps.test.ts` imports the real one. | [ryw-conversational](./planning/ryw-conversational.md) stage 7a | When a facet grows `slots?` / `denySlots?`, move `COACH_WRITABLE_SLOTS_IN_REFUSED_GROUPS` onto the grant. When `BaseCapability` (or a `withExposure()` helper) applies the grant's facets before `execute`, delete the parse-and-check block from the capability. | **open — filed [daybreak#169](https://github.com/human-centric-engineering/daybreak/issues/169)** (2026-07-27, `enhancement`). Not a defect: the enforcement helper works, and nothing forces a capability to call it. It is worth filing because the docs read as though it is automatic, which is exactly how this leaf got it wrong, and because the cost is invisible — a grant that enforces nothing looks identical to one that does. |
| **A missing capability binding is permissive, so "revoke a grant" and "delete the row" are opposites.** `dispatch()` synthesizes a **default-allow** binding when no `AiAgentCapability` pivot row exists (`lib/orchestration/capabilities/dispatcher.ts`), and `loadExposureConfig` returns `PERMISSIVE` for "no binding / `customConfig` null" (`lib/framework/data-slots/capabilities/exposure.ts`). Both defaults are individually reasonable and backward-compatible. Together they mean the intuitive way to withdraw a capability — delete the grant — leaves it dispatchable **and unrestricted**, where before it was dispatchable and pinned. The streaming handler dispatches the tool name the model emits without checking it against the advertised list, so an unadvertised name still reaches this path; a conversation resumed across the change can carry the model's own earlier calls to the removed tool. **The leaf carries no core code:** `004-reclaim-coach-grants` disables rather than deletes, and ensures a disabled row exists for any retired capability. | [ryw-conversational](./planning/ryw-conversational.md) stage 7a | If a missing binding ever defaults to denied, or the handler refuses tool names outside the agent's advertised set, drop the `RETIRED` block from the seed and let absence mean absence. | **open — filed [sunrise#476](https://github.com/human-centric-engineering/sunrise/issues/476)** (2026-07-27, `upstream-gap`), cross-referenced to daybreak#169 for the exposure half. Not a defect — permissive-on-missing is the right default for a platform where most agents have no per-binding config at all. It is worth filing because the failure is silent and inverted: the operator action that reads as "remove this permission" is the one that widens it, and nothing in either default's docstring says so. Found by `/security-review` on the commit that removed a grant. |

## Defects found in Daybreak

_F3 `ryw-firstlight` t-1/t-2 ran boot → register → sync → publish → resolve →
stream end to end against real Postgres with **zero Daybreak-framework bugs** —
the two integration gaps it surfaced are seam/ergonomics asks in the ledger
above, not behavioural defects, and the `>3 → re-scope F4` circuit-breaker did
not trip. F4 t-4's consumer chat client then found one **Sunrise-core** defect
(below Daybreak) by being a real streaming consumer — logged below. Lesson
recorded in [`planning/planning-retro.md`](./planning/planning-retro.md) §A._

_A later Daybreak-tier defect (daybreak#160, 2026-07-25) surfaced outside F3's run —
the module registry is lost at request time under Next 16 + Turbopack — exactly the
"anything the build trips over afterwards belongs here too" case above. Logged below._

_F8 `ryw-access` added a third **Sunrise-core** defect (sunrise#466) and it is the most
consequential so far: unverified email change, found by `/security-review` because the
leaf had just started keying access on that column. Note the pattern across all three —
none was found by reading core. Each surfaced because a real consumer depended on core
behaving the way its shape implies._

| Defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Found by                                                                                | Repro                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The shared client SSE schema `chatStreamEventSchema` (`components/admin/orchestration/chat/chat-events.ts`, Sunrise core) omits the **`budget_exceeded_per_turn`** variant, though `streamChat` (`lib/orchestration/chat`) emits it. On the tool-loop-abort path it is the **sole terminal frame** (no trailing `done`/`error`), so `parseChatStreamEvent` returns `null` and every consumer — the admin `ChatInterface` reference included — silently drops it, leaving an empty assistant turn with no explanation. `content_reset` **is** modelled, so only the budget event leaks.                                                                                                                                                                                                                                                                                                                                                                                              | F4 t-4 `ryw-shell` (consumer coach-chat client)                                         | Send a message whose tool loop exceeds the per-turn USD cap; the handler yields `{ type: 'budget_exceeded_per_turn', … }` then returns; `sse.ts` closes cleanly on return (no `TERMINAL_ERROR_FRAME`, which only fires on throw). Client union has no such variant → dropped. Leaf works around it by falling back to the raw `parseSseBlock` frame in `coach-chat.tsx` (shared infra, no core edit).                                                                                                    | **open — filed [sunrise#461](https://github.com/human-centric-engineering/sunrise/issues/461)** (2026-07-25). Filed on **Sunrise** (not Daybreak): the schema, `streamChat`, and `sse.ts` are all Sunrise core, so it goes straight to the tier that owns them. Fix upstream: add `z.object({ type: z.literal('budget_exceeded_per_turn'), code: z.string().optional(), message: z.string(), usedUsd: z.number().optional(), limitUsd: z.number().optional() })` to `chatStreamEventSchema`. When it lands, drop the leaf's raw-frame fallback in `coach-chat.tsx` and handle the modelled variant.                                                                                                                                                                                                                                                                     |
| The module registry (`lib/framework/modules/registry.ts`) keeps registrations in a **bare module-scoped `Map`**, not `globalThis`-backed. `registerModule()` runs only from `initApp()` → `initLeafApp()` at `instrumentation.ts` boot, but under **Next 16 + Turbopack the instrumentation module graph is separate from the route-handler/RSC graph**, so the boot-populated `Map` is a _different, empty_ instance at request time. Every request-time read (`getRegisteredModule`, `getRegisteredModules`) sees an empty registry. The admin module Config tab (`getModuleConfigForm` → `registered: getRegisteredModule(slug) !== undefined`) therefore renders **"This module's code is no longer registered"** and blocks A4 config editing (I11) for an active, DB-synced module.                                                                                                                                                                                           | This session (admin module detail) — first request-time consumer of the module registry | Server dev log, one process, 16 s apart: boot logs `syncRegisteredModules … {registered:1, retired:0}`; request then logs `Framework module config form read … {registered:false, fields:0}`. `framework_module.isRegistered = true`, module `active` throughout; restart doesn't help (every boot registers into the instrumentation context). Leaf carries a `globalThis` **keep-mine** fix in `lib/framework/modules/registry.ts` (mirrors `globalForPrisma`, `lib/db/client.ts`).                    | **open — filed [daybreak#160](https://github.com/human-centric-engineering/daybreak/issues/160)** (2026-07-25). Framework-tier — Daybreak owns `lib/framework/**`. Fix upstream: back the registry `Map` with `globalThis`. When it lands, delete the leaf keep-mine block in `registry.ts` and restore the plain `Map`. The audit this prompted — the other instrumentation-populated in-memory registries — **confirmed two more affected surfaces** (context contributors + capability dispatch handlers), both Sunrise-core; see the next row (sunrise#462). Guard-floor/guard-event contributors ride the same seam and are covered by that fix.                                                                                                                                                                                                                   |
| The chat context-contributor map (`lib/orchestration/chat/context-builder.ts`) and the capability dispatcher singleton (`lib/orchestration/capabilities/dispatcher.ts`) — **both Sunrise core** — are bare module-scoped, so a framework tier's boot-time registrations are lost at request time under the same Turbopack instrumentation/route split. The request-path self-heals cover only Sunrise built-ins + the **app** tier (`initAppContextContributors`; `registerBuiltInCapabilities` → `initAppCapabilities`), never a framework tier registering at `initFramework()`/`syncFramework()` boot. → a module-bound coach agent silently loses both its module `LOCKED CONTEXT` block **and** its framework tools (`get_state`, `fill_slot`, guidance/engagement/emergence — `getCapabilityDefinitions` filters out any slug absent from the in-process dispatcher).                                                                                                         | This session — audit prompted by daybreak#160                                           | Same split as daybreak#160 (proven for the sibling module registry). Framework registrations run only in `initFramework()`/`syncFramework()` (instrumentation graph); request-path lazy inits re-register app + built-ins only. Confirmed by wiring, not yet a live failure — no coach chat turn had exercised the tool-loop path on this boot, so it would bite silently on first use. Leaf carries `globalThis` **keep-mine** fixes in both core files (mirror `globalForPrisma`, `lib/db/client.ts`). | **open — filed [sunrise#462](https://github.com/human-centric-engineering/sunrise/issues/462)** (2026-07-25). Sunrise-core — core owns `lib/orchestration/**` (label `upstream-gap`). Fix upstream: `globalThis`-back both singletons. When it lands, delete the leaf keep-mine blocks in `context-builder.ts` and `dispatcher.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **`PATCH /api/v1/users/me` (`app/api/v1/users/me/route.ts`, Sunrise core) lets any authenticated account change its own `email` with no re-verification and without clearing `emailVerified`** — the handler writes the new address straight through and better-auth's verification flow never runs. Any fork that keys entitlement, invitations, allow-lists or team membership on email therefore keys it on a **user-controlled, self-asserted** field that still reads as verified. → concretely, in this app a standard-tier account could rename itself onto a pending **client**-tier invite's address, redeem it at the run-creation gate, take a 12-month unlimited-audit window, and leave the intended recipient unable to redeem their own invite.                                                                                                                                                                                                                      | [F8 `ryw-access`](./planning/ryw-access.md) t-2                                         | When Sunrise requires re-verification on email change (send to the new address, keep the old until confirmed, set `emailVerified: false` meanwhile), drop the leaf's two compensating conditions in `lib/app/programme/access/grants.ts` and let redemption match on email alone. Keep the deterministic redemption key regardless — that is idempotence, not authorisation.                                                                                                                             | **open — filed [sunrise#466](https://github.com/human-centric-engineering/sunrise/issues/466)** (2026-07-25, `bug` + `upstream-gap`). Found by `/security-review` on F8 t-2 and rated **High**; **fixed leaf-side in #41** without touching core — redemption additionally requires that no unconsumed invitation token remains for the address (core deletes it on acceptance, so an outstanding one proves the account did not arrive through that invite) **and** that the account does not predate the invitation, with a regression test. The leaf fix closes the hijack for _this_ app; the field is still rewritable, so the row stays open until core re-verifies. Lesson in [`planning/planning-retro.md`](./planning/planning-retro.md) §B — a leaf that keys access on a user-editable field inherits the platform's edit rules as part of its threat model. |
| **No framework code carried — a canary on core's internals instead.** `lib/orchestration/llm/structured-completion.ts` persists nothing, which is the property F5's calendar categorise depends on: event summaries go to a model, only per-bucket hour totals come back and are stored, and **I4 promises publicly that no meeting title is ever kept**. But nothing in that file promises non-persistence — its docstring commits only to being a "neutral LLM utility — no evaluation coupling, no Next.js imports", which is about layering. Adding prompt logging or eval replay upstream would be consistent with everything it says about itself and would break a downstream privacy claim without touching leaf code. **The leaf carries no core code:** `tests/unit/invariants/calendar-privacy.test.ts` asserts the file references no database client, so the day it acquires one, `leaf:checks` fails on the next PR.                                                  | [F5 `ryw-calendar`](./planning/ryw-calendar.md) t-2, answered at [[planning/post-v1     | post-v1]] P10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When the non-persistence is stated in the contract (or guarded upstream), delete the canary and cite the guarantee instead. If Sunrise decides these calls SHOULD be durable, move the categorise step off `runStructuredCompletion` — that is the outcome I4 cannot survive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **open — filed [sunrise#472](https://github.com/human-centric-engineering/sunrise/issues/472)** (2026-07-26, `upstream-gap` + `enhancement`). **F5's plan asked this exact question and nobody recorded an answer** — "note it if t-2 finds the non-persistence is incidental rather than contractual". Read at the close-out audit: it is incidental. Not a defect and not currently broken; it is a user-facing promise resting on an undocumented property of somebody else's function. |
| **The authenticated surface has no seam — an app cannot link to its own product, or land a user on it.** Two halves of one gap. (1) `components/layouts/protected-nav.tsx` carries a hardcoded nav array (Dashboard / Profile / Settings / Admin), while `public-nav.tsx` three files away already reads the fork scaffold `lib/app/public-nav.ts` — Sunrise decided a fork owns its _marketing_ nav and did not extend that to the nav its actual users see. (2) `/dashboard` is hardcoded as the post-auth destination across **eight sites in seven files** (login + OAuth fallbacks, signup push + OAuth prop, accept-invite push + OAuth prop, verify-email replace, `proxy.ts`), with matching copy "Redirecting to dashboard…". **The leaf carries local shims:** [`lib/app/protected-nav.ts`](../../lib/app/protected-nav.ts) and [`lib/app/auth-landing.ts`](../../lib/app/auth-landing.ts), plus a one-line `??` read in `protected-nav.tsx` and the eight literal swaps. | post-v1 — found in use, not in a feature                                                | When Sunrise adds the scaffolds (`lib/protected-nav/types.ts` + `lib/app/protected-nav.ts` mirroring the public-nav pair, and `lib/app/auth-landing.ts`), delete both shims, revert `protected-nav.tsx` to the platform read, and set the seam values instead. Keep [`tests/unit/components/layouts/protected-nav.test.tsx`](../../tests/unit/components/layouts/protected-nav.test.tsx) either way — the component having no test is half of why this was invisible.                                    | **open — filed [sunrise#473](https://github.com/human-centric-engineering/sunrise/issues/473)** (2026-07-26, `upstream-gap` + `enhancement`). **The only row here found by using the product rather than building it.** A leader followed a group invite link, claimed a seat, set a password — and landed on stock account scaffolding with no route to the programme they were invited to. Every part of the access chain was correct; the door simply had no handle. No test caught it because `ProtectedNav` had no test, and no review caught it because there was no seam whose emptiness would look wrong. The asymmetry with `public-nav.ts` is the strongest evidence: the pattern already exists one file away.                                                                                                                                               |

## Adding a row

1. Add the row (our change or the defect, the owning feature, the concrete
   delegate/fix action, status).
2. File it against **the tier that owns the code**, as the feature's own Done-when
   deliverable — a fork-perspective note on an existing issue if one tracks it,
   otherwise a new one. **Framework-tier** asks (`lib/framework/**`, the boot/seed
   chain, journey engine) go to **Daybreak**
   (`human-centric-engineering/daybreak`). **Sunrise-core** asks (core `lib/`,
   `app/api/v1`, orchestration/chat, `components/**`) go to **Sunrise**
   (`human-centric-engineering/sunrise`, label `upstream-gap`) — they reach us
   _through_ Daybreak, but the fix lands in the tier that owns the file, so file it
   there directly.
3. Close the row on the pull that lands it, once our copy is deleted and callers
   delegate.
